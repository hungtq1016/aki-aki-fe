// import { i18n } from "@/core/services/base/translation"
// import { computed, ref } from "vue"
// import type { Header } from "vue3-easy-data-table"
// import { itemsSelected } from "./table.body"

// export const countItems = computed(() => itemsSelected.value.length)
// export const search = ref('')
// export const selectedHeaders = ref<Header[]>([...props.headers])

// export const localeHeaders = computed(() => {
//     return selectedHeaders.value.map((header) => {
//         return {
//             ...header,
//             text: i18n.global.t(header.text) as string
//         }
//     })
// })
